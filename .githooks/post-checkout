#!/bin/sh
# Optional local hooks — enable with: python scripts/install_githooks.py
# $3=1 when switching branches (not file checkout).
if [ "$3" = "1" ]; then
  python3 scripts/fetch_remotes.py --local-sync || exit 1
fi
